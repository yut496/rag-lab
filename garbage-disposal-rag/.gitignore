# ingest が生成する成果物（大田区サイトのスクレイプ内容・バイナリ索引）。
# 再配布を避けるためコミットしない。手元での観察用。
index.npz
chunks_dump.txt
chunks_sample*.txt
corpus_date.txt

# ゴールデンセット本体（手作業作成・ideal_answer に大田区FAQ本文を含むため再配布回避）。
# 雛形 goldenset_template.json はスクレイプを含まないため別途コミットする。
goldenset.json

# Phase 4 観測ログ（top_k本文・生成回答＝スクレイプ派生。再配布回避でコミットしない）。
# observation_summary.md は本文を含まない設計のため別途コミットする。
observation_log.jsonl
