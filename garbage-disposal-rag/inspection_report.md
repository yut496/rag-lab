# コーパス整形 inspection report — Phase 2（大田区ごみRAG）

崩れを「黙って直して綺麗な結果だけ出す」ことはしていない。気づいた崩れは下記に全て記録し、
各々「直したか / 直していないか / どう直したか」を明記する。迷った箇所は直さず選択肢を併記する。

## コーパス取得
- 取得日（スナップショット）: **2026-06-20**
- SOURCES: `index.html`（家庭から出る資源とごみ）配下の **41ページ全件**
- 生成チャンク: **379**（`ota_index.npz`）。ページあたり 4〜29 件、**0件ページ無し**。

## 本文抽出（使ったセレクタ）
- **全41ページ `<main>` タグで抽出**（大田区は `<main>` 内に本文を配置）。`[skip]` ログ 0＝全ページ抽出成功。
- セクション分割は `h2/h3`、見出しを `【…】` でチャンク先頭に付与（**見出し欠落 0 件**）。
- `<table>` は行を分断しない専用処理（`_table_to_rows` / `split_table_keep_header`）。表チャンクは
  **19 件 / 7 ページ**（gomishigen, purasuhcikunodashikata, kogata, haishoku,
  gomi-syori-ken_shop-list, doubutsushitai, zaitaku-iryou-haikibutsu）。

---

## 崩れ① 本文以外のページ構成要素が混入【未修正・要判断】
`<main>` をそのまま走査する実装の副作用で、本文でない要素がチャンク化されている。

- **(a) ページメタ**: 各ページ先頭チャンクに `ページ番号：653170450` / `更新日：2025年12月3日`。
  更新日は有用かもしれないが「ページ番号」は検索上ノイズ。
- **(b) 評価フィードバック欄**: 末尾に
  `【このページについて】お寄せいただいた評価は運営の参考といたします。 ごみ減量推進課`
  （全ページ共通ウィジェット）。
- **(c) 末尾の関連リンク一覧**: 例（環境ポスターページ末尾）
  `…にご注意ください / 雑がみ回収袋の作り方 / コードレス掃除機用非純正のバッテリーパックについて`
  ＝他ページへのリンク `li` が本文末尾に混入。

**対応: 直していない。** `<main>` 直下から特定要素（フィードバック欄・関連リンク領域）を除去すれば
消せるが、各ページの DOM クラスを確認せず除去すると本文の誤除去リスクがある。判断を委ねたい。

**選択肢（いずれか / 組合せ）:**
- A: `「このページについて」` セクション見出し以降を一律カット（評価欄を確実に除去）。
- B: `ページ番号：` / `更新日：` 行を除去（更新日は `last_verified` があるので冗長）。
- C: 段落でない純リンク `li`（本文でない一覧リンク）を除去。
- D: 現状維持（検索ノイズは増えるが情報の欠落はしない）。

## 崩れ② 表の住所が複数列に分断【未修正・要判断】
店舗一覧表（`gomi-syori-ken_shop-list`）で、HTML 上 住所が「丁目」と「番地」で別セルになっている。
```
№ ｜ 取 扱 所 名 称 ｜ 所  在  地 ｜ 管轄
1 ｜ 城南運送事業協同組合 ｜ 平和島五丁目 ｜ 11番1号 ｜ 大森清掃事務所
```
- **行は保持できている**（行分断はしていない）。ただし住所が `平和島五丁目 ｜ 11番1号` と
  `｜` で分かれ、列としての連続性は分断表示になる。
- **対応: 直していない。** 元の表セル構造由来。隣接セルの機械的結合は表ごとに正しさが変わる
  （見出し行や別意味の列まで連結する恐れ）ため。
- 選択肢: A: 列の隣接セルを連結する汎用ルールを入れる（誤連結リスク有） / B: 現状維持。

## 崩れ③ 見出しの二重括弧 `【…【…】】`【未修正・軽微】
`section` 名自体に `【】` を含むページで、`split_keep_header` が再度 `【】` で包むため二重になる。
- 例: `【ご家庭から出る【可燃ごみ、不燃ごみ】】原則、通常の…`（7 件）。
- **対応: 直していない。** 表示上のみで検索影響は軽微。気になるなら見出しの `【】` を剥がす一行で対処可。

---

## 崩れていない点（観察として記録）
- **表内の品目と注記は同一行で保持**。表対応の効果で、注記が品目から切り離されていない。例:
  `おもちゃ(プラスチック製) ｜ プラスチック ｜ プラスチックだけでできているものに限る。※中に電池が入っているものは出せません`
- **見出し欠落 0 件**（全チャンクが `【…】` で始まる）。
- **文の途中切れは基本なし**: `split_keep_header` が `。` 境界で分割するため。最長 731 字＝句点の無い
  単一長文が 1 件あるのみ（中央値 125 字）。

## 本文が薄いページ（抽出失敗ではない）
`家電(kaden)` / `ペット・動物死体(doubutsushitai)` / `強風時(kyoufuji)` などは先頭チャンクが
「ページ番号＋更新日」のみで本文が乏しい。これは元ページが PDF / 画像主体で HTML 本文が少ないため
であり、抽出側の崩れではない（チャンクは生成されている）。

## まとめ
- 41 ページ全て抽出成功、致命的な抽出失敗は無し。
- 最大の改善余地は **崩れ①（本文以外の混入）**。検索ノイズになるため、上記 A〜D から方針を決めてほしい。
- 表の行保持・注記保持は機能。列分断（崩れ②）は元 HTML 構造由来。
- 二重括弧（崩れ③）は軽微。
